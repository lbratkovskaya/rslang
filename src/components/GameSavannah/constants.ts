import { IOptions } from '../../store/types';

export const SELECT_LEVELS: IOptions = {
  name: 'level',
  amount: 6,
};

export const SELECT_ROUNDS: IOptions = {
  name: 'round',
  amount: 30,
};

export const MAX_LENGTH_GAME_ARR: number = 20;
